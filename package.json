{
  "name": "hydraai-inspect",
  "private": true,
  "version": "0.1.0",
  "description": "HydraAI Inspect monorepo tooling",
  "scripts": {
    "lint": "echo 'run lint via package scripts'",
    "format": "prettier --check .",
    "format:write": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
